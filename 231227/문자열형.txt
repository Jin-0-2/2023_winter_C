문자열형
타입        (무엇을, 어떤 크기로?)
int      |정수를, 4byte (sizeof(int))
-------------------------------------------------
string   |문자열, 크기 가변적 -> 기본형 타입 제공 불가 -> 종결문자('\0')

문자열 선언
char arr[5]; // 문자열 저장 공간(문자char 5개)