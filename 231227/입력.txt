입력

int a; // 저장공간 생성
          // 주소가 할당 됨
int b;

// 대입연산
a = 10; L(저장공간) = R(값)
b = a;                    = 값

scanf("%d", &a); // %d: 정수를 얻어오겠다.		&a (주소연산자)
                        // a  : 여기(반드시 주소)에 넣어주겠다.